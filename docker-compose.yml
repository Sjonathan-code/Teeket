services:
  postgres:
    image: postgres:16-alpine
    container_name: teeket-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-teeket}
      POSTGRES_USER: ${POSTGRES_USER:-teeket}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-teeket_dev_password}
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-teeket} -d ${POSTGRES_DB:-teeket}']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
